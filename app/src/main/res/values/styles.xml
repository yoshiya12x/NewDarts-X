<resources>

    <!--Default Style-->
    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@android:color/black</item>
        <!-- add Style -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--Setting Style-->
    <style name="SettingTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/SettingActionBarStyle</item>
        <item name="colorControlActivated">@color/colorSubPrimary</item>
        <item name="colorSwitchThumbNormal">@android:color/white</item>
        <item name="android:colorForeground">@android:color/white</item>
    </style>

    <!--Setting ActionBar-->
    <style name="SettingActionBarStyle" parent="android:Widget.Holo.ActionBar.Solid">
        <item name="android:titleTextStyle">@style/SettingActionBarTitleStyle</item>
        <item name="android:background">@android:color/black</item>
        <item name="android:backgroundStacked">@android:color/black</item>
    </style>

    <!--Setting ActionBar Title-->
    <style name="SettingActionBarTitleStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--TextView-->
    <style name="CustomTextView" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">normal</item>
    </style>

</resources>
